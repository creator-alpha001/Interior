/**
 * Which professional the portal is signed in as, until real auth exists.
 *
 * Aarohi is used because she works across two trades, which is what makes the
 * per-domain behaviour visible — separate ratings, separate lead pools, and a
 * combined agreement where one customer hired her for both.
 */
export const CURRENT_PROFESSIONAL_ID = "pro-aarohi";
export const CURRENT_PROFESSIONAL_USER_ID = "user-pro-aarohi";
