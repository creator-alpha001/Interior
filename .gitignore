node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo
next-env.d.ts
# Every env file, then the committed templates back. This repository is public,
# and `.env.production` — the obvious name for the Supabase and Hostinger
# credentials — was matched by none of the three patterns that used to be here.
.env
.env.*
!.env.example
!.env.production.example
.DS_Store
Thumbs.db
.vercel
coverage/
backups/

# The credentials tracker. This repository is public: the file holds sign-ins
# today and will hold the MSG91 key, the R2 secret and the production database
# passwords tomorrow. It must never become a tracked file.
CREDENTIALS.md

# Local storage and the SMS outbox, written by the local delivery drivers.
.data/

# A local backup of .env, made when repointing at a non-Docker Postgres.
.env.*.bak

# Filled-in copies of the Supabase bootstrap, holding real role passwords.
*.local.sql
